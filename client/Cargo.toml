[package]
name = "client"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
leptos = { version = "0.8.10", features = ["csr"] }
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"

[package.metadata.trunk]
public_url = "/"
